<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healventureapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealventureApp">
        <activity android:name=".view.MainPageActivity"></activity>
        <activity android:name=".view.BookAppointment"></activity>
        <activity android:name=".view.Feedback"></activity>
        <activity android:name=".view.AboutUs"></activity>
        <activity android:name=".view.ForgotPassword"></activity>

        <activity
            android:name=".view.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>